你现在是资深UI设计规范架构师，我会提供一套参考系统的页面视觉、交互、组件细节描述，你基于我给的参考系统信息，输出一份标准化、可直接交付AI复刻同款视觉风格的完整设计规范文档，输出格式必须是纯HTML代码，带基础CSS内联样式，页面清晰分栏、层级分明，无需引入外部资源，打开即可阅读。

这份设计规范核心目标：任何AI拿到这份HTML规范文件，都能1:1复刻参考系统的视觉、交互、页面布局风格，不能缺少任何可复用设计约束。

规范文档HTML内必须包含以下12大核心模块，每个模块有明确说明、数值规范、使用示例、禁止项：
1. 【品牌基础视觉规范】
主色、辅助色、中性色、危险/成功/警告/信息色色值（HEX+RGB）；色彩使用场景限制；渐变规范；阴影规范；透明度层级；禁止使用的色彩搭配
2. 【字体排版规范系统】
字体家族（标题/正文/辅助文字）；字重分级；字号阶梯（H1-H6、大正文、常规正文、小字、备注）；行高、字间距标准；段落间距；文字颜色对应规则；中英文混排规则
3. 【布局栅格规范】
页面版心宽度、左右边距；栅格列数、列宽、沟槽间距；卡片内边距标准；全局统一内外边距阶梯（4/8/12/16/20/24px等）；弹窗/侧边栏固定尺寸；响应式断点规则（PC/平板/移动端）
4. 【圆角&描边规范】
全局圆角分级（极小、小、常规、大圆角、全圆角）；描边粗细分级；描边颜色使用场景；玻璃拟态/边框风格统一规则
5. 【图标规范】
图标线条粗细、尺寸阶梯；图标色系；填充/线性图标使用区分；图标统一圆角；禁止异形图标
6. 【基础原子组件规范】
按钮（主按钮/次要/文字/危险/禁用/悬浮点击态）、输入框、下拉选择、单选、复选框、开关、分页、标签、提示气泡；每个组件包含：尺寸、色彩、圆角、阴影、hover/active/disabled交互样式
7. 【复合模块组件规范】
卡片、表格、弹窗、导航栏、侧边菜单、搜索栏、筛选面板、状态标签、进度条、空状态、加载状态；每种模块完整视觉+交互规则
8. 【页面通用交互规则】
hover悬浮效果、点击反馈、过渡动画时长、弹出动画、滚动样式、遮罩层透明度、loading动画风格
9. 【图片&素材规范】
图片圆角、统一滤镜、占位图风格、头像尺寸规范、图片阴影规则
10. 【全局阴影分层规范】
一级轻阴影（卡片）、二级中等阴影（弹窗下拉）、三级重阴影（全局浮层），统一色值与模糊数值
11. 【页面模板布局规范】
列表页、详情页、首页、弹窗页、表单页通用布局结构、区块分割方式、标题区布局、操作栏位置规则
12. 【复刻约束禁令】
明确列出所有禁止修改的视觉规则，AI复刻时必须严格遵守，不允许自主调整色彩、圆角、间距、字体

HTML输出要求：
1. 整体页面白底，分左侧导航目录+右侧内容区双栏布局；
2. 每个模块用大标题区分，规范数值用灰色代码块展示；
3. 色彩规范区域用彩色色块div直观展示色值；
4. 所有尺寸、色值、参数全部量化为固定px/HEX数值，不允许模糊描述；
5. CSS全部写在<style>标签内，不引用外部css、js、图片资源；
6. 文档末尾增加【AI复刻使用说明】，告诉AI读取本规范复刻页面的执行步骤；
7. 代码完整可直接复制到本地.html文件打开，无遗漏、无注释冗余。

接下来我会发给你参考系统的页面视觉细节，你严格按照上述结构输出完整HTML设计规范文档。